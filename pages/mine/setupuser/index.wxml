<view class="content">
  <view class="header">
    <input placeholder="输入您要搜索的用户" bindblur="search"></input>
    <image class="icon" src="/images/search.png"></image>
  </view>

  <view class="item" wx:for="{{list}}">
    <view class="logo">
      <image src="/images/logo1.png"></image>
    </view>
    <view class="msg">
      <text class="name">陈鸿宇</text>
      <text>大家都假的假的假的假的假的假的就XX小区 3幢 603室</text>
    </view>
    <view class="itembto" data-index="{{index}}" bindtap="changestate">
      解除绑定
    </view>
  </view>
</view>
<view class="modal-mask" bindtap="hideModal" catchtouchmove="preventTouchMove" wx:if="{{showmodal}}"></view>
<view wx:if="{{showmsg}}" class="tixing">
  <image src="/images/close.png" bindtap="hideModal" class="close"></image>
  <text>确定与用户陈鸿宇取消绑定？</text>
  <view class="caozuo">
    <view class="caozuo1" bindtap="submit">确 定</view>
    <view class="caozuo2" bindtap="hideModal">取 消</view>
  </view>
</view>